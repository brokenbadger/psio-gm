# PSIO-GM Docker Compose
#
# Web UI (recommended in Docker — no X11):
#   mkdir -p games
#   PSIO_GAMES_DIR=./games docker compose --profile web up --build
#   Open http://127.0.0.1:5000 and scan /games (or a subdirectory).
#
# Tk GUI (optional, needs host X11):
#   xhost +local:docker
#   PSIO_GAMES_DIR=./games docker compose --profile gui up --build
#   xhost -local:docker
#
# Custom games folder (use ./ or an absolute path — bare names are treated as volumes):
#   PSIO_GAMES_DIR=/home/you/ps1/games docker compose --profile web up --build

name: psio-gm

services:
  psio-gm-web:
    profiles: ["web"]
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: psio-gm-app:0.2
    container_name: psio-gm-web
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - type: bind
        source: ${PSIO_GAMES_DIR:-./games}
        target: /games
        bind:
          create_host_path: true
    working_dir: /app/src
    command: ["python", "-m", "webapp", "--host", "0.0.0.0", "--port", "5000"]

  psio-gm:
    profiles: ["gui"]
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: psio-gm-app:0.2
    container_name: psio-gm
    environment:
      DISPLAY: ${DISPLAY:-:0}
      XAUTHORITY: /root/.Xauthority
    volumes:
      - type: bind
        source: /tmp/.X11-unix
        target: /tmp/.X11-unix
      - type: bind
        source: ${XAUTHORITY:-${HOME}/.Xauthority}
        target: /root/.Xauthority
        read_only: true
      - type: bind
        source: ${PSIO_GAMES_DIR:-./games}
        target: /games
        bind:
          create_host_path: true
    working_dir: /app/src
    ipc: host
    stdin_open: true
    tty: true
