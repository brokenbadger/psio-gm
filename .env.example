# Optional. Prefer passing IDs on the command line (works for any user):
#   PUID=$(id -u) PGID=$(id -g) docker compose up --build
#
# Or set them here permanently for this machine:
PUID=1000
PGID=1000

# Optional: override the host games folder (default ./games)
# PSIO_GAMES_DIR=./games
